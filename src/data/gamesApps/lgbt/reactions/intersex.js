import flagsInfo from '../flagsInfo';

const reactions = [
	[
		{
			name: flagsInfo.intersex.name,
			number: flagsInfo.intersex.number,
			img: flagsInfo.intersex.img,
			why: ''
		},
		{
			name: flagsInfo.torik.name,
			number: flagsInfo.torik.number,
			img: flagsInfo.torik.img,
			why: ''
		},
		{
			name: flagsInfo.tranfen.name,
			number: flagsInfo.tranfen.number, 
			img: flagsInfo.tranfen.img,
			why: ''
		}
	]
];

export default reactions;
