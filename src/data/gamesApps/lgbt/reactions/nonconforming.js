import flagsInfo from '../flagsInfo';

const reactions = [
	[
		{
			name: flagsInfo.genderNonConforming.name,
			number: flagsInfo.genderNonConforming.number,
			img: flagsInfo.genderNonConforming.img,
			why: ''
		},
		{
			name: flagsInfo.aromantic.name,
			number: flagsInfo.aromantic.number,
			img: flagsInfo.aromantic.img,
			why: ''
		},
		{
			name: flagsInfo.libragender.name,
			number: flagsInfo.libragender.number,
			img: flagsInfo.libragender.img,
			why: ''
		}
	]
];

export default reactions;
